# 보고서 재발 장애: 수정 전 계획과 인수 기준

## 관측과 한계

배포 후 새 샘씨엔에스 요청은 20:03:25부터 20:18:43까지 실행되어 기본 여섯 장,
DART 세 장, 투자전략까지 생성했으나 첫 최종 검수의 일반 오류에서 중단됐다.
기존 운영 로그에는 실제 검수 응답이 없어, 결합된 조건 중 어느 항목이 실패했는지는
확정할 수 없다. 이전의 초안 재사용 검증 성공을 신규 운영 요청 성공으로 일반화하지 않는다.

## 변경 전에 확인한 구조적 문제

1. 읽기 전용 장의 충돌도 보고하라는 지침과 이를 형식 오류로 거절하는 수신 범위가 다르다.
2. 충돌 진단 전에 아직 발행하지 않을 요약·수정안까지 검사해 유효한 진단을 잃을 수 있다.
3. 운영에는 세부 원인을 재현할 안전한 기록이 없지만, 검증 도구에만 캡처 기능이 있다.
4. 주요 통합 테스트가 요약과 복구 함수를 함께 대체하여 실제 경계 연결을 검증하지 못한다.

## 실행 계획

- 단계별 상태·JSON 형식·진단 가능한 장·수정 권한을 분리한다. 장별 권한은 한 목록에서 정의한다.
- 기존 SDK의 구조화 출력 경로를 사용하고 실제 OAuth 경로에서 지원 여부부터 확인한다.
- 충돌 응답에는 발행용 요약이나 편집을 요구하지 않는다. READY에서만 요약·편집 보호를 적용한다.
- 모든 알려진 장의 충돌을 표현할 수 있게 하되, 원문·계산표·peer·macro 근거는 자동 수정하지 않는다.
- 모델이 쓴 기본 장·DART 장은 기존 작성자와 같은 원문으로 제한적으로 복구한다. 전략은 수정한
  근거에서 새로 합성한다. 무제한 재생성·새 자료 수집·보호를 끄는 성공 처리는 금지한다.
- 전략 작성 전에 사실 충돌을 평가하고 최종 검수도 유지한다. 초안 복구와 전략 재합성의 횟수는
  각각 제한하며, 남은 충돌·형식 위반·원천 부족은 서로 다른 사유로 명시적으로 종료한다.
- 오류 코드와 안전한 비공개 재현 자료를 크기·개수·기간 제한으로 저장한다. 비밀값·환경 설정은
  기록하지 않으며 기록 실패가 원래 오류를 덮어쓰지 않게 한다.

## 인수 기준

- 형식 오류, 알려진 비복구 충돌, 전략 단독, 근거 부족, 수치·기간·부호·용량 문제를 구별한다.
- 실제 구조화 응답 → 검증기 → 복구 → 새 전략 → 실제 재검수 연결을 시험한다.
- 실패·시간 초과·취소에서 부분 PDF·정상 캐시·채널 메시지가 발행되지 않음을 확인한다.
- 동일 입력 회귀와 신규 전체 생성을 분리한다. 최종 후보에서 초안/캐시를 주입하지 않는
  한국 report 경로를 실제 앱 환경으로 실행하고 별도 표본을 확인한다.
- 한국·미국 진입점과 캐시·전달 계약을 함께 확인한다. 실제 주문은 만들지 않는다.
- PR의 정확한 head와 CI를 확인한 뒤 clean ff-only로 배포한다. 운영 소스·서비스 점검과
  첫 운영 요청 또는 첫 정규 배치의 관측 여부를 구분해서 보고한다.

손절·매수 점수·주문 수량·매매 정책 변경, 데이터가 불충분한 보고서의 정상 발행은 범위 밖이다.

## 9월 25일 후속 수정 범위

- 신규 앱 진입 테스트의 교정 사유에 코드와 설명이 섞인 응답을 확인했다. 사유를
  기존 네 코드의 enum으로 제한하고 로컬 검증도 같은 목록을 사용한다. 임의 문자열을
  접두사만 보고 정상 사유로 바꾸지 않는다.
- 뉴스 작성자가 만든 Competitive Evidence와 코드가 복사한 overview Handoff의
  소유권을 구분한다. 원문 공시·계산표는 계속 불변이다. 정확한 내용·ID·경계를 확인한
  파생 복사본만 분리하고, 기존 뉴스 복구 후 ID와 복사본을 한 번 재생성한다.
- 검수 입력에서는 검증된 중복 복사본만 생략한다. 뉴스 원본과 독립적인 개요 본문은
  검수하며, 복사본 불일치는 숨기지 않고 실패시킨다. 새 ID는 사실 인증이 아니다.
- 미국 PDF의 공통 시장 근거에서 측정값이 전혀 없는 선택 지표 행은 생략하고
  미확인 범위를 묶어 안내한다. SPY 기준·부분 측정·국면·관측 모집단의 한계는 유지하며
  원시 수치·국면 판정·매수 게이트는 변경하지 않는다.
- 밤사이 미국 매수보류 분석은 별도 Evidence Packet으로 수행한다. 거래량 필터 추가는
  이번 장애 수리와 분리하며, 표본과 시점 정의 검토 없이 전역 조건으로 승격하지 않는다.

추가 인수 기준은 CE 교정 후 뉴스와 복사본 일치, 멱등성, 변조 거절, 실제 PDF의
미측정 행 정리, 정확한 최종 코드의 신규 앱 진입 테스트다. 이전 실패 기록은 보존한다.

## 반복 실행 후 확인한 의미·출처 경계

- `db930a73`의 SK텔레콤 신규 서비스 실행은 1,167초에 MD/PDF 저장까지 완료했다.
  샘씨엔에스는 유효 날짜 `2026.08.26`을 금융 숫자 `2026.08`로 읽은 검증기 오탐으로
  중단됐다. 완전한 유효 날짜의 구분자만 처리하고, 부분·잘못된·부호 있는 날짜는
  금액 검사에서 제외하지 않는다. 당시 출력문을 바꾸지 않은 재검사에서 신규 수치와
  URL은 0개였다. 이 재검사는 신규 생성 성공이 아니다.
- `53ad167e` 신규 실행은 최종 의미 검수에서 두 종목 모두 실패했다. 성공한 이전
  샘플을 이 커밋의 성공으로 합산하지 않는다. 반복 횟수나 사실 검증을 완화하지 않는다.
- 한국 시장 장은 미국과 달리 인용 연결 검사가 합성 앞단에 없었고, 기존 helper도
  공통 시장 packet이 없는 일반 report에서는 검사하지 않았다. 생성·캐시 초안을 모두
  합성 전에 검사하고 연결되지 않은 번호 인용의 거시 주장 자체를 제외한다. 원시 지수
  계산값은 shared_reference에 보존한다. URL 존재가 사실 자체의 인증은 아니다.
- 배수 비교는 PBR/PER 구분뿐 아니라 실적/예상·기간·업종 기준·회계 범위가 일치해야
  한다. 업종 기준이 없으면 숫자는 별도로 인용하되 상대 고평가 단정은 하지 않는다.
- SK텔레콤 74.6%는 무조건 잘못된 값이 아니었다. 같은 공시 URL을 새로 조회한 연결
  손익계산서의 반기 누적 지배주주 귀속 순이익은 793,012/454,060백만원이며 74.6%와
  부합한다. 전체 순이익 782,395/444,822백만원과는 귀속 범위가 다르다. 자동 검수에
  특정 수치를 예외 등록하지 않고, 귀속 범위를 집필·교정·검수의 공통 계약으로 명시한다.
  진단의 문장은 단순 순이익이라고 썼으므로 귀속 기준의 명확화가 필요하다.

원문 확인: 2026-09-24 22:53:59 UTC에 공시 뷰어의 동일 URL을 새로 조회했다.
이는 최초 실행의 원시 snapshot과 byte equality를 확인한 것이 아니다.
SHA-256: `066f297c310a33ac2c981d3984077b04ec2c33e66133feb91e3414b1d3d5c022`.
공시 식별자 `20260813001728`, 문서 `11526165`, 요소 `19`의 연결 손익계산서다.

## 교정 책임과 고정 상한

`f7abac4e`에서 SK텔레콤은 신규 생성 1,034초에 MD/PDF 저장까지 완료했다.
샘씨엔에스는 앞서 기업·뉴스·수급을 고친 뒤 처음 지적된 가격 장을 전역 예산 때문에
교정하지 못했다. 휴장 정보가 최종 검수기에만 제공되는 입력 비대칭도 확인했다.

- 로컬 달력은 agent 생성 전에 한 번 계산하고 같은 payload를 작성·복구·검수·합성에
  전달한다. 독립 호출만 기존 로컬 조회를 사용한다. 달력 미확인은 휴장으로 바꾸지 않는다.
- 최신 이동평균의 수준·배열과 시간에 따른 기울기는 구분한다. 기울기가 확인되지
  않았다고 하락으로 판정하거나 새 매매 조건을 만들지 않는다.
- 보고서 전체 초안 교정은 최대 두 차례 묶음이며, 각 기본 장 소유자당 한 번만 가능하다.
  기본 작성자 추가 호출 총수는 최대 6회, DART 재작성 묶음은 최대 1회다.
- 이미 교정한 소유자가 다시 지적되면 즉시 중단한다. 새 소유자와 섞인 요청도 추가
  호출 전에 거절한다. 최종 단계에 들어가도 사용 이력·상한을 초기화하지 않는다.
- 전략 재합성 재시도는 최대 한 번이다. CE 복사본은 별도 소유자·교정 기회가 아니다.
- 이 고정된 구조에서도 남는 충돌은 발행 차단하며, 성공을 위해 상한을 더 늘리지 않는다.

완료 보고는 코드 검사·신규 실행·PDF 검수·운영 배포·첫 정규 배치를 별개로 구분한다.
이전 성공과 이후 코드를 합쳐 하나의 성공으로 표현하지 않는다.

## 주식 수량 표기의 정확한 동치

`a1aeb49a`에서는 샘씨엔에스가 1,124초, 미국 AAPL이 362초에 신규 MD/PDF 저장을
완료했다. SK텔레콤은 원문의 `121만 9,730주 순매도`를 출력의
`순매수 수량 -1,219,730주`와 비교하면서 숫자 토큰만 달라 중단됐다.

표기 동치는 주식 순매수 수량 타입 안에서만 허용한다. 정수 만 단위·나머지·주 단위와
명시한 방향을 확인하며 원화·비율·반대 방향·근사·범위·불명확한 부호로 확장하지 않는다.
복합 수량의 일부 숫자를 독립된 재무수치로 허용하지 않고, 파생 수량을 단위 없는 전역
숫자 집합에 추가하지 않는다. 여러 수량을 나열한 순매수 수량 필드는 각 추가 항목의
부호와 주 단위가 명시된 경우만 처리한다. 생략된 공동 방향을 추정하지 않는다.

실제 실패 출력문을 바꾸지 않은 검증에서 새 수치·URL은 없었다. 이는 해당 표기
오탐의 재현 검증이며 새로운 전체 생성·사실 인증을 대신하지 않는다. 교정 횟수 상한은
변경하지 않았다. AAPL의 기존 보유/신규 진입 구분이 모호한 전략 문장과 CE 부록의
내부형 필드 표시는 별도 내용·표현 검토 항목이며, 신규 생성 성공만으로 모든 투자
전략이 검증됐다고 주장하지 않는다.
