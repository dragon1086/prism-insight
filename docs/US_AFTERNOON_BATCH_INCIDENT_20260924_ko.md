# 미국 오후 배치 누락 및 한국·미국 통합 점검

## 확인된 장애와 범위

2026-09-24 03:30 KST(미국 거래일 2026-09-23) 오후 배치는 시작·매크로 분석을 통과했다. 현재 스냅샷 518종목, 전일 스냅샷 1종목만 확보한 뒤 세 트리거가 모두 빈 결과를 반환했고 03:34에 보고서·알림·tracking 전에 종료했다. 저장 JSON에도 universe 518 / valid 1 / missing 517이 남아 있다. 이를 정상적인 신호 없음으로 단정할 수 없다.

직전 배포의 831a3f6c→aa2f1e89에서 미국 스냅샷·트리거·오케스트레이터 파일은 바뀌지 않았다. 보고서 패치가 해당 스크리닝 로직을 직접 변경했다는 근거는 없다. 당시 공급자 원본 응답은 보존되지 않아 실제 열 구조 오류인지 대량 결측인지 확정하지 않는다.

## 재현 및 수정 계획

1. 실제 미국 공급자 모듈에서 MultiIndex 양방향·단일/다중 종목 flat·정확한 거래일·NaN·부분 응답을 검증한다. 모호한 flat 데이터를 첫 종목으로 간주하거나 이전 날짜를 바꾸어 표시하지 않는다.
2. 현재 스냅샷의 이미 수신한 며칠치 이력을 같은 종목·기준일·가격 기준의 전일 조회에 단회 재사용해 중복 대량 요청을 줄인다. 캐시 조건이 다르거나 만료되면 기존 별도 조회 경로를 사용한다.
3. 누락을 수집 상태로 명시한다. 유효한 부분집합의 기존 조건은 유지하되, 자료 부족을 정상 후보 없음으로 알리지 않는다. 임의 커버리지 백분율을 새 매수 차단 조건으로 만들지 않는다.
4. 거래대금 필터 후 빈 DataFrame에 전일 Series가 다시 들어오던 오전/오후 정렬 예외를 고친다. 필터 기준은 변경하지 않는다.
5. 후보 없음·수집 실패·보고서 실패·PDF 실패가 침묵 또는 성공 완료로 기록되지 않도록 단계별 상태 알림을 보강한다. 빈 산출물로 과거 pending 메시지를 발송하지 않는다.
6. 한국·미국 실제 orchestrator 모듈의 오전/오후 정상·부분 실패·빈 결과·전송·tracking 연결을 주문/외부 발송 없이 검증한다. 기존 보유 관리 호출 빈도는 별도 확인 없이 확대하지 않는다.

## 안전 경계

- 정규 배치/실제 매매를 수동 재실행하지 않는다. 당시 손익·체결을 현재 자료로 재구성하지 않는다.
- 모델·브로커·Telegram/Kakao 외부 경계를 fixture로 대체하고 네트워크를 차단한다. 기존 코드/실제 모듈 연결을 우회한 테스트만으로 완료하지 않는다.
- 가격 공급자 스모크는 제한된 읽기 전용 조회로만 수행한다. 변경 코드는 exact-head CI·Git ff-only 배포·운영 Python 점검을 거친다.
- 첫 정규 배치의 새 버전 결과는 테스트와 구분해 보고한다.

## 변경 검토와 회귀 범위

- 변경 유형은 공급자 데이터 해독·정렬·완료 알림의 오류 수정이다. 기준선은 aa2f1e89이며 신규 전략, 선발 임계값, 위험 예산, 손절 규칙을 도입하지 않았다.
- 미국 시세 입력에서 정확한 날짜와 종목을 확인하고, 기존 트리거 → 결과 JSON → 실제 orchestrator → 보고서 파일 → PDF/요약 경계 → tracking 전달을 검증한다. 한국도 실제 orchestrator의 오전·오후 흐름과 부분 실패를 검증한다. 모델 생성·브로커·외부 전송은 대체하므로 모델 품질·실제 체결 성공을 증명하는 테스트는 아니다.
- 빈 후보의 tracking 호출을 새로 추가하지 않았다. tracking([])도 기존 보유 종목 SELL 판단을 실행하므로 이 장애 수정에서 호출 빈도를 확대하지 않는다. 정상 산출물의 기존 tracking 연결은 유지한다.
- 이미 수신한 시세 이력 재사용으로 직전일 중복 대량 요청을 줄였다. 직전일이 원래 응답에 없으면 누락 상태를 보존하며, 다른 날짜의 가격으로 채우지 않는다.
- 전송 대상을 이번 배치 파일로 한정했다. 기존 `.sending` 선점·전달 결과 불명확 시 재전송 금지는 보존한다.
- 같은 종목의 과거 요약도 생성 전후 파일 fingerprint가 바뀌지 않으면 이번 메시지로 포함하지 않는다. 과거 파일은 삭제하지 않고 최신 PDF와 기존 tracking 경로는 유지한다.
- 필수 CI에 공급자 계약·양국 실제 파이프라인·상태 메시지·발송 범위 회귀를 추가했다. 전체 오류가 사라졌다는 보장이나 수익성 개선 증거로 해석하지 않는다.

## 배포 전 검증

- 공급자 계약·상태 안내·메시지 범위·기존 전달 안전성: 47 passed.
- 한국 실제 오전/오후 파이프라인: 9 passed.
- 미국 실제 오전/오후 파이프라인 및 트리거 오류 진단: 19 passed.
- 기존 KR/US 공급자 형태 통합 및 기업행동 입력: 47 passed.
- 기존 보고서 병렬성·NASDAQ 구성·전달 진단: 18 passed.
- 합계 140개 통과. 신규 파일 Ruff, 변경 운영 파일 치명적 정적 검사, compileall, git diff --check 통과.
- 운영 시세 스모크·배포·첫 정규 배치 관측은 이 로컬 검증과 별도다.
