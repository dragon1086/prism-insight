# Trigger Reliability Card — Implementation Plan

> **Version**: 1.0.0 | **Created**: 2026-02-08 | **Branch**: `feat/trigger-reliability`

## Overview

PRISM의 3가지 데이터 소스(성과 추적, 매매 원칙, 실제 매매)를 **트리거 유형** 축으로 교차 결합하여,
사용자에게 "어떤 신호를 더 믿을 수 있는가"라는 행동 가능한 인사이트를 제공한다.

### Data Lineage (확인 완료)

```
analysis_performance_tracker ─── 분석한 모든 종목의 7/14/30일 후속 가격 추적
        ↓ trigger_type으로 GROUP BY
trading_history ────────────── 실제 매수→매도 완료된 매매 기록
        ↓ trigger_type으로 GROUP BY
trading_principles ─────────── 매도 후 저널 → lessons → 원칙 추출
        ↓ trigger_type과 직접 연결은 없으나, 원칙의 condition 텍스트로 매칭 가능
```

### 노출 위치

| 위치 | 설명 |
|------|------|
| 인사이트 탭 상단 | 트리거 신뢰도 카드 (full) |
| 대시보드 탭 | 미니 배지 ("가장 신뢰할 트리거: X") |
| 텔레그램 봇 `/triggers` | 텍스트 요약 |
| 텔레그램 채널 주간리포트 | 주 1회 자동 발송 |
| 매매 알림 메시지 | "(이 트리거 과거 승률: N%)" 한줄 추가 |

---

## Phase 정의

### Phase 0: 프로젝트 셋업 + 브랜치

| 항목 | 내용 |
|------|------|
| **작업** | feature 브랜치 생성, docs 구조 확인 |
| **산출물** | `feat/trigger-reliability` 브랜치, 이 문서 |
| **검증** | `git branch --show-current` = feat/trigger-reliability |

### Phase 1: 백엔드 데이터 레이어 (KR + US)

| 항목 | 내용 |
|------|------|
| **작업** | `generate_dashboard_json.py`와 `generate_us_dashboard_json.py`에 `get_trigger_reliability()` 메서드 추가 |
| **입력** | SQLite: `analysis_performance_tracker`, `trading_history`, `trading_principles` (KR) / `us_*` 버전 (US) |
| **출력 데이터 구조** | 아래 참조 |
| **검증 기준** | `python3 -c "..."` 로 함수 직접 호출 → JSON 출력 확인 |
| **테스트** | `tests/test_trigger_reliability.py` 단위 테스트 |

**실제 데이터 현황 (2026-02-09 확인):**

| 시장 | 테이블 | completed | pending/in_progress | 비고 |
|------|--------|-----------|---------------------|------|
| KR | analysis_performance_tracker | 13건 | 171건 | 거래량 급증 8건 완료가 최다 |
| KR | trading_history | 63건 | - | 42건은 trigger_type=NULL (2026-01-12 이전) |
| KR | trading_principles | 96건 | - | US 원칙 테이블 없음, KR만 존재 |
| US | us_analysis_performance_tracker | 0건 | 59건 | 모두 pending |
| US | us_trading_history | 2건 | - | 데이터 극소 |

**KR 주요 트리거 유형 (실제 이름):**
- `거래량 급증` — 55건 추적 (8 completed), 실매매 없음
- `갭 상승 모멘텀 상위주` — 23건 추적, 실매매 5건 (승률 60%)
- `기술적 돌파` — 17건 추적 (1 completed), 실매매 없음
- `거래량 증가 상위 횡보주` — 15건 추적, 실매매 3건 (승률 33%)
- `시총 대비 집중 자금 유입 상위주` — 15건 추적, 실매매 2건 (승률 50%)
- `마감 강도 상위주` — 11건 추적, 실매매 7건 (승률 0%)
- `일중 상승률 상위주` — 11건 추적, 실매매 4건 (승률 75%)
- `AI분석` (trigger_type=NULL) — 실매매 42건 (승률 48%)

**US 주요 트리거 유형:**
- `Gap Up Momentum Top` (36건), `Intraday Rise Top` (6), `Volume Surge Sideways` (6), `Volume Surge Top` (6), `Closing Strength Top` (5)

**KR/US 스키마 차이점:**
- KR: `tracked_30d_return`, `tracking_status='completed'`
- US: `return_30d`, `return_30d IS NOT NULL` 로 완료 판단
- US에는 `us_trading_principles` 테이블 없음 — KR `trading_principles`만 존재 (market='KR')
- KR/US 모두 같은 DB 파일 (`stock_tracking_db.sqlite`)

**출력 데이터 구조:**
```json
{
  "trigger_reliability": [
    {
      "trigger_type": "거래량 급증",
      "grade": "B",
      "analysis_accuracy": {
        "total_tracked": 55,
        "completed": 8,
        "avg_30d_return": 0.044,
        "win_rate_30d": 0.625
      },
      "actual_trading": {
        "count": 0,
        "win_rate": null,
        "avg_profit_rate": null,
        "profit_factor": null
      },
      "related_principles": [
        {
          "condition": "거래량 2배 이상 확인 후 진입...",
          "confidence": 0.7,
          "supporting_trades": 2
        }
      ],
      "recommendation": "분석 정확도 양호. 실매매 데이터 축적 필요."
    }
  ],
  "best_trigger": "거래량 급증",
  "last_updated": "2026-02-09T10:00:00"
}
```

**등급 산정 로직:**
```
A: 분석 승률 >= 60% AND 실매매 승률 >= 60% AND 실매매 >= 5건
B: 분석 승률 >= 50% AND (실매매 승률 >= 50% OR 실매매 < 5건)
C: 분석 승률 < 50% OR 실매매 승률 < 50%
D: 데이터 부족 (분석 completed < 3건)
```

**원칙 매칭 로직:**
- `trading_principles`에는 직접적인 trigger_type 컬럼이 없음 (KR만 존재, US 원칙 테이블 없음)
- Phase 1에서는 원칙의 `condition` 텍스트에서 트리거 키워드 매칭 사용:
  ```python
  TRIGGER_KEYWORDS = {
      "거래량 급증": ["거래량", "volume", "수급"],
      "갭 상승 모멘텀 상위주": ["갭", "gap", "모멘텀", "momentum"],
      "기술적 돌파": ["돌파", "breakout", "저항", "지지"],
      "일중 상승률 상위주": ["급등", "상승률", "intraday"],
      "마감 강도 상위주": ["마감", "장 마감", "closing"],
      "거래량 증가 상위 횡보주": ["횡보", "sideways", "레인지"],
      "시총 대비 집중 자금 유입 상위주": ["자금 유입", "수급", "시총"],
  }
  ```
  - 매칭 안 되면 "공통 원칙"으로 분류
- US는 원칙 테이블이 없으므로 `related_principles: []` 반환

### Phase 2: 대시보드 인사이트 탭 — 트리거 신뢰도 카드

| 항목 | 내용 |
|------|------|
| **작업** | `trading-insights-page.tsx`에 트리거 신뢰도 카드 컴포넌트 추가 |
| **위치** | 기존 "Performance Analysis Summary" + "Trading Wisdom Summary" 박스 **위** |
| **타입 정의** | `types/dashboard.ts`에 `TriggerReliability` 타입 추가 |
| **i18n** | `locales/ko.json`, `locales/en.json`에 번역 키 추가 |
| **검증 기준** | `npm run build` 성공 + 브라우저에서 카드 렌더링 확인 |
| **테스트** | 빌드 에러 없음, 타입 에러 없음 (`lsp_diagnostics_directory`) |

### Phase 3: 대시보드 메인 탭 — 미니 배지

| 항목 | 내용 |
|------|------|
| **작업** | 대시보드 메인 탭 상단에 "가장 신뢰할 트리거: volume_surge (A등급)" 미니 배지 |
| **파일** | `app/page.tsx` 또는 관련 요약 컴포넌트 |
| **검증 기준** | 빌드 성공 + 메인 탭에서 배지 표시 확인 |
| **테스트** | 타입 에러 없음 |

### Phase 4: 텔레그램 봇 `/triggers` 명령어

| 항목 | 내용 |
|------|------|
| **작업** | `telegram_ai_bot.py`에 `/triggers` 핸들러 추가 |
| **응답 포맷** | 트리거별 한줄 요약 텍스트 (마크다운) |
| **KR/US** | 동일 DB `stock_tracking_db.sqlite` (US 테이블은 `us_` 접두어) |
| **검증 기준** | `python3 -c "..."` 로 메시지 생성 함수 직접 호출 → 텍스트 출력 확인 |
| **테스트** | 봇 실행 없이 메시지 생성 함수만 단독 테스트 |

**응답 예시:**
```
📡 트리거 신뢰도 리포트

🇰🇷 한국시장
  B  거래량 급증 — 분석 63%, 매매 데이터 부족
  B  일중 상승률 상위주 — 매매 75% (4건)
  C  마감 강도 상위주 — 매매 0% (7건)
  D  기술적 돌파 — 데이터 부족

🇺🇸 미국시장
  D  Gap Up Momentum Top — 추적 중 (36건)
  D  Volume Surge Top — 추적 중 (6건)

💡 거래량 급증 신호의 분석 정확도가 가장 높습니다
```

### Phase 5: 주간 리포트 + crontab

| 항목 | 내용 |
|------|------|
| **작업** | `weekly_insight_report.py` 신규 스크립트 생성 |
| **기능** | 주간 트리거 성과 요약 → 텔레그램 채널 발송 |
| **데이터** | 이번 주 새로 추적 완료된 종목, 트리거별 성과 변화, 새 원칙 |
| **KR/US** | 하나의 스크립트에서 양쪽 DB 모두 처리 |
| **crontab** | 매주 일요일 10:00 KST 실행 |
| **검증 기준** | `python3 weekly_insight_report.py --dry-run` 실행 → 메시지 텍스트 출력 확인 |
| **테스트** | dry-run 모드로 텔레그램 발송 없이 메시지 생성만 확인 |
| **운영 가이드** | `docs/trigger-reliability/OPERATION_GUIDE.md` |

**주간 리포트 포맷:**
```
📋 PRISM 주간 인사이트 (2/3 ~ 2/8)

🇰🇷 한국시장
━━━━━━━━━━━━━━━━━━━━
🛡️ 회피한 손실: 3건 (평균 -13.2%)
❌ 놓친 기회: 1건 (+18.5%)
📊 가장 정확한 트리거: 거래량 급증 (승률 63%)
📌 새 원칙: 1개 추가 (총 97개)

🇺🇸 미국시장
━━━━━━━━━━━━━━━━━━━━
🛡️ 회피한 손실: 1건 (-11.5%)
❌ 놓친 기회: 0건
📊 가장 정확한 트리거: 데이터 축적 중
📌 새 원칙: 0개

💡 이번 주 핵심: 거래량 급증 트리거 신뢰도 유지 중
```

### Phase 6: 매매 알림 메시지에 트리거 승률 추가

| 항목 | 내용 |
|------|------|
| **작업** | 기존 매매 신호 알림에 "(이 트리거 과거 승률: N%)" 한줄 추가 |
| **파일** | `stock_tracking_enhanced_agent.py` (KR), `prism-us/us_stock_tracking_agent.py` (US) |
| **위치** | 텔레그램/Pub/Sub으로 알림 발송하는 부분 |
| **데이터** | `analysis_performance_tracker`에서 해당 trigger_type의 completed 승률 조회 |
| **검증 기준** | Grep으로 알림 메시지 생성 부분 확인 → 한줄 추가 확인 |
| **테스트** | 기존 알림 포맷 깨지지 않는지 확인 |

---

## 수정 대상 파일 목록

### Phase 1 (Python 백엔드)
- `examples/generate_dashboard_json.py` — `get_trigger_reliability()` 추가
- `examples/generate_us_dashboard_json.py` — 동일 메서드 US 버전
- `tests/test_trigger_reliability.py` — 신규 테스트

### Phase 2 (대시보드 인사이트 탭)
- `examples/dashboard/components/trigger-reliability-card.tsx` — 신규 컴포넌트
- `examples/dashboard/components/trading-insights-page.tsx` — 카드 import + 배치
- `examples/dashboard/types/dashboard.ts` — 타입 추가
- `examples/dashboard/locales/ko.json` — i18n
- `examples/dashboard/locales/en.json` — i18n

### Phase 3 (대시보드 메인 탭)
- `examples/dashboard/app/page.tsx` 또는 요약 컴포넌트 — 미니 배지

### Phase 4 (텔레그램 봇)
- `telegram_ai_bot.py` — `/triggers` 핸들러 추가

### Phase 5 (주간 리포트)
- `weekly_insight_report.py` — 신규 스크립트
- `docs/trigger-reliability/OPERATION_GUIDE.md` — crontab 가이드

### Phase 6 (매매 알림)
- `stock_tracking_enhanced_agent.py` — 알림 메시지에 승률 추가
- `prism-us/us_stock_tracking_agent.py` — US 버전 동일

---

## Phase 진행 규칙

1. **순서 준수**: Phase 0 → 1 → 2 → 3 → 4 → 5 → 6
2. **검증 후 진행**: 각 Phase의 검증 기준을 모두 통과해야 다음 Phase로 이동
3. **Phase 완료 기록**: 각 Phase 완료 시 이 문서의 체크리스트 업데이트
4. **KR+US 동시**: 각 Phase에서 KR/US 모두 구현 후 완료 처리

---

## Progress Checklist

- [x] Phase 0: 프로젝트 셋업 + 브랜치
- [x] Phase 1: 백엔드 데이터 레이어 (KR + US) — 15/15 테스트 통과 (2026-02-09)
- [x] Phase 2: 대시보드 인사이트 탭 — 트리거 신뢰도 카드 (npm run build 성공, 타입 에러 없음, 2026-02-09)
- [x] Phase 3: 대시보드 메인 탭 — 미니 배지 (npm run build 성공, 2026-02-09)
- [x] Phase 4: 텔레그램 봇 /triggers 명령어 (standalone 테스트 통과, 2026-02-09)
- [x] Phase 5: 주간 리포트 + crontab (dry-run 테스트 통과 + OPERATION_GUIDE.md 생성, 2026-02-09)
- [x] Phase 6: 매매 알림 메시지 트리거 승률 추가 (KR+US 4파일 수정, syntax check 통과, 2026-02-09)
